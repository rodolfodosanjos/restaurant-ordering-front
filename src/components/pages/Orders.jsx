import React from 'react';
import OrderFormDialog from './OrderFormDialog.jsx';

export default () => (
	<div>
		<OrderFormDialog 
			orderToUpdate={{}}
			onClose={() => {}}
			open={true}/>
	</div>
);